import { Spinner as Loader } from "@nextui-org/spinner";

const Spinner = () => {
  return (
    <div className="flex justify-center items-center h-full w-full">
      <Loader />
    </div>
  );
};

export default Spinner;
